# 广告设计项目协作平台开发规范

本文件适用于整个 `advertisement_Design` 仓库，是后续开发、重构、审查和文档维护的强制约束。产品与技术基线分别为 `docs/需求分析报告_2.0.md` 和 `docs/技术设计文档_2.0.md`。

## 当前产品方向

平台采用“项目优先、会话承载、七阶段持续服务”模式。客户登录后直接进入需求沟通工作台；第一条有效需求原子创建项目和独立会话。旧版“作品顾问、咨询需求单、三秒匹配、六十秒确认”不再是目标业务流程。

禁止为新功能继续扩展旧咨询匹配主线。现有 `consultation` 代码、表和 API 在迁移完成前可以保留，但不得未经调用方、数据和回滚验证直接删除。可复用能力应迁入新的业务所有者模块，不得仅通过改名延续错误模型。

客户侧团队消息统一显示“项目服务团队”。不得向客户展示 Agent、模型名称、工具调用、自动回复、转人工、设计师接管、内部成员或运行诊断；也不得创建虚构员工姓名、头像、成员列表或在线状态。统一显示身份不得抹去后台真实主体和完整审计。

## 仓库结构

```text
advertisement_Design/
├── advertisement_Design_back/       # Java 后端模块化单体
├── advertisement_Design_user/       # 访客与客户桌面端
├── advertisement_Design_stylist/    # 设计师与管理控制端
├── docs/                            # 项目文档
└── CLAUDE.md
```

当前只实现桌面端。除非需求基线明确更新，不新增移动端、小程序、语音消息或音视频通话范围。

## 技术栈

后端固定使用 Java 17、Spring Boot 3.2、MyBatis-Plus 3.5、MySQL 8.0、Lombok、MapStruct、Jakarta Validation、Spring Security、JWT 和 Swagger/OpenAPI。

前端使用 Vue 3、Vite、TypeScript、Vue Router、Pinia、Axios 和 Element Plus。新代码必须符合现有子项目构建工具和目录约定，不得在未评估影响时更换框架。

## 后端架构

后端是模块化单体：一个 Spring Boot 应用、一个主要部署单元和一个数据库，不因模块增多而拆分微服务。

目标顶层业务模块为：

```text
com.advertisementdesign.back
├── auth
├── identity
├── portfolio
├── project
├── communication
├── workflow
├── artifact
├── commercial
├── automation
├── notification
└── common
```

模块职责以《技术设计文档_2.0.md》为准。按实际需要创建包，不生成没有代码和数据生命周期的空模块或空分层。

禁止新增全局 `controller`、`service`、`mapper`、`entity`、`dto`、`vo` 或 `api` 技术分层。代码必须归入业务模块。`common` 不得包含项目、消息、合同、阶段或 Agent 的业务规则，也不得成为杂物目录。

模块之间不得直接调用对方 Mapper、读写对方 Entity 或业务表。跨模块使用公开应用服务、命令／查询 DTO、公开 VO 或领域事件；禁止把 Entity 作为模块契约，禁止循环依赖。

## 分层规则

模块内部按需要使用 `controller`、`service`、`mapper`、`entity`、`dto`、`vo`、`enums`、`converter`、`repository` 等目录。

Controller 只负责接收请求、Jakarta Validation 基础校验、调用本模块应用服务并返回 `Result<T>`。禁止在 Controller 编写业务规则、SQL、状态迁移、数据权限、密码处理、外部回调业务或重复 try/catch。

Service 负责业务用例、事务边界、权限检查、状态迁移、本模块 Mapper 协调和跨模块公开服务调用。事务优先定义在完整业务用例上。不要机械地为每个 Service 创建接口和 `Impl`；仅在多实现、明确边界或测试替身确有价值时使用接口。

Mapper 位于所属模块并继承 MyBatis-Plus `BaseMapper<T>`。优先使用 Wrapper 和 Java Config。只有 SQL 确实复杂时才使用 Mapper XML，并在技术文档中说明原因。

## 对象与代码规范

Controller 必须统一返回 `Result<T>`，不得直接返回 Entity。请求使用 Request DTO，响应使用 Response VO，跨模块使用命令／查询 DTO 或公开 VO。常规对象转换优先使用模块内 MapStruct Converter。

实体使用 Lombok `@Data` 和 `@Builder`；需要无参或全参构造时补充 `@NoArgsConstructor`、`@AllArgsConstructor`。数据库字段使用下划线，Java 字段使用驼峰。通过 `@TableName`、`@TableId` 和必要的 `@Version` 明确映射。

生成 Java 代码时必须补齐正确包名、import、注解、泛型、访问修饰符和可编译实现。禁止占位 import、伪代码式生产实现和无业务依据的默认成功结果。

## API 规范

所有接口统一以 `/api` 开头，Controller 类和方法必须添加 Swagger/OpenAPI 注解。新增接口应同步补齐 DTO/VO、Service、必要 Mapper／Repository、Converter、权限校验、错误码和适量测试。

认证使用 `/api/auth`，作品使用 `/api/portfolio-cases`，项目使用 `/api/projects`。会话、阶段、成员、产物、确认、商业、接管、暂停和终止等动作应处于 `/api/projects/{projectId}/...` 上下文中，禁止使用裸资源 ID 绕过项目授权。

关键命令需支持幂等键或业务唯一约束，并携带对象版本。电子签和支付回调使用独立受保护入口，必须验签和幂等处理，不能复用客户“确认成功”接口。

包迁移和内部重构不得随意改变已有 API 契约。需要替换时先记录保留、兼容、迁移和下线策略。

## 项目、会话与命名规则

一个项目对应一个独立会话，且拥有独立阶段、成员、文件、版本、合同、付款和审计历史。

打开空白工作台或点击“新建需求”不得创建服务端项目或临时会话。只有第一条有效需求才创建项目和会话；创建操作必须原子且幂等。纯问候、表情或无用途说明附件不属于有效需求。

项目初始名称为“新项目需求沟通”。自动命名只能在名称来源为自动且信息稳定时更新，不得每条消息频繁变更。客户手动改名后必须将名称来源设为手动，任何自动流程不得覆盖，除非客户显式恢复自动命名。

## 七阶段工作流

阶段顺序固定为：需求引导、签订合同预付款、资料调研报告、草图风格敲定、审稿定稿、交付尾款、售后复购。

项目状态和阶段状态必须分离。阶段状态只能通过 `workflow` 的明确命令和前置条件迁移。禁止 Controller、Mapper、定时任务或 Agent 直接覆盖阶段字段。

需求阶段以客户确认的不可变需求版本完成；合同预付款阶段以电子签成功和预付款成功双事件完成；调研以客户确认具体报告版本完成；草图和定稿以客户确认经设计师审核的具体版本完成；交付以真实尾款、交付版本和接收／合同超时规则完成；售后是长期状态，不强制一次性结束。

对话中的“同意”“已付”“签好了”等文字不能作为合同、付款、版本确认或交付完成依据。

允许受控回退，但必须追加重开／变更事件并保留原完成记录和所有历史版本。禁止删除后续工作或把历史状态改写成从未发生。

## 产物、版本与确认

需求、报价、合同、报告、草图、正式设计、变更单和交付物必须版本化。发布或确认后的版本不可原地修改；修订必须创建新版本并关联父版本。

确认动作必须绑定项目、对象、不可变版本、真实操作人、权限依据、时间和结果。只有主要联系人或被显式授予对应动作权限的客户成员可以执行关键确认。

任何草图和正式设计在发送客户前都必须具有主负责设计师或授权审核人的批准记录。生成式工具产生的版本默认未审核；具备发布权限的设计师人工制作或上传的版本可以在发布动作中同时批准，但必须记录真实批准人和审计。普通调研报告可按置信度、来源、敏感性和异常规则决定自动发布或人工审核。

截图标注必须绑定具体文件和版本，形成结构化反馈。历史版本不得静默覆盖或物理删除。

## 客户可见身份与真实主体

团队消息对客户统一显示“项目服务团队”。消息数据必须同时记录客户显示身份、真实 actor 类型、真实 actor ID 和发送来源。

自动与人工消息都遵循同一不可变规则：已发送消息不得静默编辑，错误应通过新更正消息处理。

设计师接管使用显式的内部接管会话。接管生效后自动回复暂停，避免并发回复；恢复前自动化服务必须读取完整会话和干预记录。客户侧不得显示接管、恢复或负责人变更。

## 自动化与工具边界

协调 Agent 和七阶段 Agent 只能调用明确授权的应用服务或工具，不得直接访问 Mapper、数据库表或绕过模块权限。

规则内普通消息、需求模板、报价、合同草稿、变更单草稿、项目计划、正常报告、提醒和阶段摘要可以自动生成或发送。

支付和签署结果必须来自真实外部事件，客户确认必须来自授权操作。退款、投诉裁定、版权争议、责任认定、已签合同终止、业务记录删除和版本化交付物删除不得由 Agent 独立执行。

低置信度、需求冲突、重复无法推进、客户明显不满、合同／支付失败、逾期、退款异常、阶段超时和 SLA 超时必须创建升级事项并通知负责人或管理员。

Agent 置信度、知识引用、失败原因、提示、工具调用和计划动作只允许在具有高级排障权限的运行诊断页面查看，不得进入普通设计师项目详情或任何客户响应。

## 权限与数据隔离

密码必须使用 BCrypt，禁止明文保存。登录使用 JWT 或同等级机制。前端路由守卫只改善体验，后端每个接口必须执行角色、组织、项目、资源和动作权限校验。

客户只能访问自己组织中明确参与的项目、会话、消息、文件和业务记录。项目成员角色包括主要联系人／管理员、可确认、仅评论和仅查看；关键动作还需专项授权。

所有设计师可以读取所有项目的必要脱敏摘要。只有主负责设计师、有效授权协作设计师或管理员可以读取完整会话、敏感文件和商业明细；接管、修改、审核和负责人调整还需对应动作权限。禁止把“全局摘要可见”实现成“所有设计师完整数据可见”。

管理员权限应按业务管理、合同、财务、审计和运行诊断细分。不得用一个无边界超级角色替代权限设计。

跨客户知识和历史经验必须先进行租户隔离、权限过滤与必要脱敏。不得在客户之间泄露私有聊天、文件、合同、价格或设计资产。

## 安全与密钥

禁止在代码、Git、日志、异常、响应、测试快照或文档中写入密码、密码哈希、验证码、完整 Token、JWT 密钥、数据库密码、邮件密码、OSS AccessKey、支付密钥或电子签密钥。

真实配置只能通过环境变量、密钥管理系统或被 Git 忽略的 `application-local.yml` 提供。不得读取真实值后复制进示例、文档或回复。发现已暴露或曾提交的凭据，必须标记为安全问题并要求通过供应商控制台吊销和轮换，不能只从 Git 删除。

前端不得持有 OSS AccessKey，也不得直接绕过后端权限上传。文件访问和下载授权必须由拥有业务关系的模块校验。公开作品与私有项目文件应采用公私 Bucket 或等价隔离策略。

## 文件归属

`common.storage` 只负责文件资产元数据和物理存储能力。`communication` 管理聊天附件关系与访问权限；`artifact` 管理需求、报告、设计和交付文件；`commercial` 管理合同及商业附件。

底层模块可以引用同一文件资产 ID，但不得把消息附件、合同文件和交付文件视为同一种业务对象。物理删除必须遵守保留期、合同、审计和争议锁定规则。

## 事务、事件与并发

项目创建、关键确认、阶段迁移、负责人变更、接管、产物发布和外部回调必须定义清晰事务边界。

关键聚合使用 `@Version`、状态条件更新或数据库唯一约束防止并发覆盖。更新行数为零时应按并发冲突处理，禁止无条件重试覆盖他人结果。

支付、签署、通知回执和关键客户端命令必须幂等。未知、处理中、失败和成功是不同状态，超时不得被推断为成功。

事务提交后才允许发送外部通知、执行 Agent 调度或调用不可撤回工具。需要可靠交付时使用 Outbox 或等价机制。定时任务必须在多实例条件下防止重复生效。

## 异常与校验

使用统一异常处理，不在 Controller 重复捕获业务异常。参数校验优先使用 Jakarta Validation，业务规则在 Service 中校验。

未认证、无权限、资源不存在和并发冲突分别保持 401、403、404 和明确冲突语义。外部工具异常应保留真实业务状态并提供可安全重试的处理方式，不得伪造成功结果。

客户错误提示使用可理解的业务语言，不暴露堆栈、SQL、内部 Agent、供应商密钥或运行诊断。

## 前端规范

客户工作台采用三栏桌面结构：左侧项目会话，中间聊天与七阶段，右侧当前阶段／项目资料／项目信息。项目名称只在项目上下文显示，不在中栏顶部无意义重复。

空白工作台必须显示欢迎语和动态需求模板卡，但欢迎、模板交互、未发送输入和附件草稿不得创建服务端项目或临时会话。模板一次突出一个相关问题，同时允许客户直接发送完整需求。

七阶段进度必须支持查看当前及历史阶段摘要。当前阶段可显示经权限计算的业务操作；历史阶段只显示完成记录和历史版本，不提供越权修改入口，也不得由前端自行推导状态迁移。

前端文案使用客户可理解的业务词语，禁止出现 Agent、模型、工具调用、低置信度、转人工和内部设计师身份。团队消息统一显示“项目服务团队”；客户消息显示真实客户项目成员名称，并由后端项目成员关系提供可信身份。

输入区支持一条消息包含多个附件，发送前必须显示附件列表并允许移除误选文件。附件发送时后端必须校验草稿所有权、文件状态、项目关系及数量／大小限制，禁止仅凭文件 ID 跨项目关联。

项目切换时必须隔离状态并处理请求竞态，禁止把旧项目响应写入新项目。关键卡片必须显示版本和权限状态，提交操作携带对象版本与幂等标识。

客户侧不得通过修改前端状态直接改变合同、付款、确认、阶段或交付事实。所有最终状态以后端授权与真实事件为准。

视觉延续现有轻商务方向：浅灰白表面、深色正文、克制暖色强调和语义化业务状态色。不得将客户工作台设计成技术控制台或普通客服系统。平台支持全天提交和自动化接续；人工审核事项必须显示可理解的状态与预计时间并遵守可配置 SLA，不得用虚构在线人员表达全天服务。

## 审计与日志

关键业务动作必须写入独立审计记录，包括消息发送、确认、签署、付款、分配、接管、审核、阶段变化、暂停、恢复、取消、变更、退款决定和工具调用。

审计记录至少包含真实 actor、客户显示身份、来源、项目、对象及版本、动作、前后状态、时间、结果和关联请求。审计采用追加方式，普通业务接口不得修改。

普通日志只记录排查所需的标识和结果，不记录私有文件正文、完整模型提示、支付敏感数据或任何凭据。

## 测试规范

测试方案必须同时考虑业务风险、任务耗时和 token 消耗，选择少量代表性用例，不追求测试数量、穷举覆盖或覆盖率数字。每个新增测试必须对应一个独立且重要的业务、安全、权限、幂等或并发风险；无法说明新增风险的测试不应编写。

优先覆盖第一条有效需求原子建项、租户与项目权限、设计师摘要／完整访问边界、版本确认、签署支付双门槛、外部回调幂等、设计审核门槛、接管互斥、关键并发状态迁移、阶段回退、交付权限和跨租户文件访问。

相似输入应合并为少量代表性场景。禁止为排列组合、重复 Mockito 调用验证、低风险实现细节或单纯提高覆盖率批量生成重复测试。

开发时只运行当前模块或受影响范围的定向测试。完整功能完成后原则上统一运行一次全量测试和启动验证；只有测试失败、实现继续变化或风险范围扩大时才重复高成本验证，避免无必要地消耗任务时间和 token。

测试不得使用真实凭据、真实客户私有数据或可被误认成生产成功的支付／签署回调。

## 文档与迁移要求

需求变化先更新 `docs/需求分析报告_2.0.md`，架构或模型变化同步更新 `docs/技术设计文档_2.0.md`。API 契约变化应更新 OpenAPI 文档；数据库变化必须提供可执行迁移脚本、数据映射、回滚或补偿说明。

旧版 `docs/需求分析报告_1.0.md`、`docs/技术文档_1.0.md` 仅作历史参考，不得作为新功能实现依据。旧代码删除前必须证明无调用、无数据依赖、迁移完成且相关回归通过。

不得在一次改动中同时进行全量包迁移、全量数据库重写和大规模新功能开发。按可验证的纵向业务能力逐步迁移，结构重构与业务行为调整尽量分开验证。
