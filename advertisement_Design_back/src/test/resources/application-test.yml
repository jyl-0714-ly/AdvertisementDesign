spring:
  datasource:
    url: jdbc:h2:mem:reliability;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: always
  mail:
    host: localhost
app:
  jwt:
    secret: test-only-secret-with-at-least-thirty-two-characters
